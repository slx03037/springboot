<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.component.security.advance.web.mapper.RoleMapper">

    <select id="findRoleNamesByUserId" resultType="java.lang.String">
        SELECT sr.role_key FROM sys_user u
                                         left join sys_user_role ur on ur.user_id = u.id
                                         LEFT JOIN sys_role sr on sr.id=ur.role_id
        WHERE u.id = #{userId} ;
    </select>

</mapper>