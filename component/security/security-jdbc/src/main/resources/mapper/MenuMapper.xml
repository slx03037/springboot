<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.component.security.advance.web.mapper.MenuMapper">

    <select id="findUserMenuById" resultType="java.lang.String">
        SELECT distinct m.perms FROM sys_user u
                                         left join sys_user_role ur on ur.user_id = u.id
                                         LEFT JOIN sys_role sr on sr.id=ur.role_id
                                         left join sys_role_menu rm on rm.role_id = sr.id
                                         left join sys_menu m on m.id = rm.menu_id
        WHERE u.id = #{userId} ;
    </select>

</mapper>