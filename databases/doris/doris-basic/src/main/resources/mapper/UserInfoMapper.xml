<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.doris.basic.mapper.UserInfoMapper">

    <insert id="batchInsert" parameterType="com.doris.basic.entity.UserInfo">
        INSERT INTO user_info (id, user_name, phone, create_time, update_time) VALUES
        <foreach collection='list' item='item' separator=','>
        (#{item.id}, #{item.userName}, #{item.phone}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>
</mapper>

