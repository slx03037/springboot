server:
  port: 8087
  servlet:
    context-path: /api

spring:
  application:
    name: spring-boot-jdbc-template
  datasource:
    url: jdbc:mysql://192.168.1.73:3306/demo?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: NJben@1234



mybatis:
  # 多模块下用classpath*:扫描所有依赖jar中的XML
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.mybatis.jdbc.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
